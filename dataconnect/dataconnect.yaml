specVersion: "v1beta"
serviceId: "portfolio-master"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "portfolio-master-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
